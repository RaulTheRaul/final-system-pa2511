import { Link } from "react-router-dom";
import { useAuth } from "../../../context/AuthContext";

const TokenDashboard = () => {

    return(
        <div>
            <p>This is the Token Dashboard</p>
        </div>
    );
};

export default TokenDashboard;